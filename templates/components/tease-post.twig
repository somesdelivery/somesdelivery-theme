<div class='teasebox'>
	<a class='teasebox__thumb' href="{{post.link}}">
		{% if post.thumbnail.src %}
			<img src="{{post.thumbnail.src | resize(800,600) }}" />
		{% endif %}
	</a>
	<div class='teasebox__info'>
		{% if not is_page %}
			<p class='teasebox__meta'>{{ post.date }}</p>
		{% endif %}
		<h2 class='teasebox__title'><a href="{{post.link}}">{{post.title}}</a></h2>
		<p class='teasebox__desc'>{{ post.get_preview(25, false, 'Citește mai departe') }}</p>
	</div>
</div>